.comments
  - if @item.comments.count == 0
    %h3 Комментариев пока нет :(
  - else
    %h3 #{@product.comments.count} Ответов
  = render :partial => "comments/comment", :collection => @item.comments
  .form_answer
    = render "comments/form"
= link_to 'К списку Товаров', products_path
